import ReactDOM from 'react-dom';

import Provider from './components/options/provider';

const css = require('./styles/options.css');

ReactDOM.render(
  Provider,
  document.getElementById('container')
);